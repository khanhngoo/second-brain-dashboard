// Built in P2 (grid + drag-to-schedule); wired to real external events in P3.
export function CalendarView() {
  return (
    <div>
      <h2 className="view-title">Calendar / Time-Blocking</h2>
      <p className="empty">Coming in P2.</p>
    </div>
  );
}
